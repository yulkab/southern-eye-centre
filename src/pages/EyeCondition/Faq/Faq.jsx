import React from 'react';
import PropTypes from 'prop-types';

const Faq = props => (
  <div>
    This is a Faq
  </div>
);

Faq.propTypes = {
  // bla: PropTypes.string,
};

Faq.defaultProps = {
  // bla: 'test',
};

export default Faq;
