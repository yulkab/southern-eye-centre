import React from 'react';
import PropTypes from 'prop-types';

const Stam = props => (
  <div>
    This is a Stam
  </div>
);

Stam.propTypes = {
  // bla: PropTypes.string,
};

Stam.defaultProps = {
  // bla: 'test',
};

export default Stam;
